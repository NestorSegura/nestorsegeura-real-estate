---
phase: 03-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/analyze/route.ts
  - src/app/[locale]/analyse/page.tsx
  - src/app/[locale]/analyse/AnalysePageClient.tsx
autonomous: true

must_haves:
  truths:
    - "POST /api/analyze with valid URL returns 200 with mock scores for performance, seo, mobile, conversion, and positioning"
    - "POST /api/analyze with invalid JSON returns 400"
    - "POST /api/analyze with invalid URL returns 422 with error message"
    - "Visiting /de/analyse shows a URL input form"
    - "Submitting the form displays animated circular score gauges inline"
    - "Response returns within 2 seconds"
  artifacts:
    - path: "src/app/api/analyze/route.ts"
      provides: "POST endpoint with Zod validation, mock scores, CORS restriction"
      exports: ["POST", "OPTIONS"]
    - path: "src/app/[locale]/analyse/page.tsx"
      provides: "Analysis tool page server wrapper fetching ctaHref"
      min_lines: 20
    - path: "src/app/[locale]/analyse/AnalysePageClient.tsx"
      provides: "Client component with URL form and SVG score gauges"
      min_lines: 80
  key_links:
    - from: "src/app/[locale]/analyse/AnalysePageClient.tsx"
      to: "/api/analyze"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/analyze"
    - from: "src/app/api/analyze/route.ts"
      to: "zod"
      via: "URL validation schema"
      pattern: "z\\.object.*z\\.string.*url"
---

<objective>
Build the /api/analyze POST endpoint returning mock website analysis scores and the /de/analyse page with a URL input form and animated SVG score gauges.

Purpose: The website analysis tool is a key lead generation feature. Visitors enter their website URL and immediately see scores showing where their site can improve. This phase uses mock scores; real PageSpeed API integration is deferred.

Output: API route handler with Zod validation and mock responses, plus a client-side analyse page with form submission and circular gauge visualization.
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-landing-page/03-RESEARCH.md

@src/app/[locale]/layout.tsx
@src/i18n/messages/de.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/analyze POST endpoint with Zod validation and mock scores</name>
  <files>src/app/api/analyze/route.ts</files>
  <action>
    Create `src/app/api/analyze/route.ts` with:

    **Body schema (Zod):**
    - Import `z` from `zod` (already available as transitive dependency)
    - Define `BodySchema = z.object({ url: z.string().url('URL muss eine gueltige Web-Adresse sein') })`

    **POST handler:**
    - Parse `request.json()` in a try/catch -- return 400 `{ error: 'Invalid JSON' }` on parse failure
    - Validate with `BodySchema.safeParse(body)` -- return 422 `{ error: parsed.error.errors[0].message }` on validation failure
    - Generate mock scores (each a random integer in a "mediocre" range so they look realistic and motivate improvement):
      - `performance`: random 55-84
      - `seo`: random 60-84
      - `mobile`: random 50-84
      - `conversion`: random 40-79
      - `positioning`: random 35-79
    - Add a `// TODO: Replace mock scores with PageSpeed Insights API call` comment above the mock generation
    - Add a `// TODO: Add Positioning score logic (niche + offer definition analysis)` comment
    - Return 200 JSON: `{ url: parsed.data.url, scores: { performance, seo, mobile, conversion, positioning } }`
    - Do NOT set `Access-Control-Allow-Origin` header -- omitting it means the browser enforces same-origin by default (per LEAD-03 requirement and research recommendation)

    **OPTIONS handler:**
    - Return 204 with no body (preflight response for same-origin)
    - No CORS headers needed since we're enforcing same-origin by omission

    **Important:** Use `NextResponse.json()` from `next/server` for responses.
  </action>
  <verify>
    `npx tsc --noEmit` passes. File exists at `src/app/api/analyze/route.ts`. Contains `POST` and `OPTIONS` exports. Contains `z.object` validation. Contains `TODO` comment for PageSpeed API. Does NOT contain `Access-Control-Allow-Origin: '*'`.
  </verify>
  <done>
    POST /api/analyze accepts `{ url: string }`, validates with Zod, returns mock scores for 5 categories. Invalid JSON returns 400, invalid URL returns 422. No CORS headers means same-origin only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build analyse page with URL form and animated SVG score gauges</name>
  <files>
    src/app/[locale]/analyse/page.tsx
    src/app/[locale]/analyse/AnalysePageClient.tsx
  </files>
  <action>
    Create `src/app/[locale]/analyse/page.tsx`:

    **Page structure:**
    - This is a client component (`'use client'`) since it needs form state and fetch
    - Import `useTranslations` from `next-intl` for UI strings (analysis.title, analysis.subtitle, etc.)
    - Page layout: centered container (max-w-2xl mx-auto), padded (px-6 py-24)
    - Use inline OKLCH styles consistent with existing blocks (not Tailwind dark: variants)

    **URL input form:**
    - Headline from translations: `analysis.title` ("Kostenlose Website-Analyse")
    - Subtitle from translations: `analysis.subtitle`
    - Single text input: `type="url"`, placeholder from `analysis.placeholder`, required
    - Submit button: `analysis.submit` text, primary purple style (`oklch(0.45 0.18 290)` bg, white text, rounded-full)
    - Loading state: disable button, show `analysis.loading` text
    - On submit: `fetch('/api/analyze', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ url }) })`
    - Handle errors: show error message from response `error` field, or generic error text
    - Store scores in state: `useState<Record<string, number> | null>(null)`

    **Score display (shown after successful response):**
    - Section heading: `analysis.resultTitle`
    - Grid layout: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8`
    - For each score category (performance, seo, mobile, conversion, positioning):
      - Render a circular SVG gauge using stroke-dashoffset animation:
        ```
        const CIRCUMFERENCE = 2 * Math.PI * 40 // radius = 40
        const offset = CIRCUMFERENCE * (1 - score / 100)
        ```
      - Background circle: `stroke="oklch(0.88 0.005 80)"`, strokeWidth 8
      - Score circle: `stroke="oklch(0.45 0.18 290)"`, strokeWidth 8, strokeLinecap round
        - `strokeDasharray={CIRCUMFERENCE}`, `strokeDashoffset={offset}`
        - CSS transition: `transition: stroke-dashoffset 1s ease 0.2s`
        - `transform: rotate(-90deg)`, `transformOrigin: 50% 50%`
      - Center text: score number, fontSize 22, fontWeight 700
      - Label below SVG: category name (capitalize, translated if translations exist, otherwise English labels)
    - Score color coding (discretion): use OKLCH hue shift based on score value:
      - 0-49: red-ish `oklch(0.55 0.2 25)`
      - 50-74: amber `oklch(0.65 0.18 80)`
      - 75-100: green `oklch(0.55 0.18 145)`
    - Animate gauges from 0 to final value on mount (start with offset = CIRCUMFERENCE, then set to calculated offset after a brief delay using useEffect)

    **Score category labels (hardcode since they're domain-specific):**
    - DE: Performance, SEO, Mobile, Conversion, Positionierung
    - EN: Performance, SEO, Mobile, Conversion, Positioning
    - ES: Rendimiento, SEO, Movil, Conversion, Posicionamiento
    - Use `useLocale()` from `next-intl` to select the right label set

    **Below scores, add a CTA:**
    - Text: "Erstgespraech vereinbaren" (or translated equivalent) linking to the siteSettings CTA URL
    - Since this is a client component and we can't call sanityFetch, hardcode a fallback href `#kontakt` and add a `// TODO: Pass ctaHref from server component or layout` comment
    - Actually, better approach: make the page a server component wrapper that fetches siteSettings and passes ctaHref to a client child. Create a `AnalyseClient` component inside the same file (or as a separate unexported component within the file) that receives `ctaHref` as prop.

    **Revised structure:**
    - Default export: async server component that fetches siteSettings for ctaHref, renders AnalyseClient
    - AnalyseClient: 'use client' component with all the form/gauge logic, receives `ctaHref` prop
    - Both can live in the same file using the pattern of a named client component and a default server export. Actually, 'use client' must be at file top. So create two sections:
      1. `src/app/[locale]/analyse/page.tsx` -- server component, fetches ctaHref, renders AnalysePageClient
      2. `src/app/[locale]/analyse/AnalysePageClient.tsx` -- 'use client', form + gauges

    Create BOTH files.

    **Important patterns:**
    - No framer-motion or GSAP -- CSS transitions only (per locked decision [02-03])
    - Inline OKLCH styles (per locked decision [02-03])
    - Use `useTranslations('analysis')` for UI strings in the client component
  </action>
  <verify>
    `npx tsc --noEmit` passes. `npm run build` succeeds. Files exist at `src/app/[locale]/analyse/page.tsx` and `src/app/[locale]/analyse/AnalysePageClient.tsx`. Client component contains `fetch('/api/analyze'` call. SVG gauge elements exist with `strokeDashoffset` animation.
  </verify>
  <done>
    Visiting /de/analyse shows a URL input form with German labels. Submitting a URL calls /api/analyze and displays 5 animated circular score gauges (Performance, SEO, Mobile, Conversion, Positionierung) with color-coded results. A CTA button below scores links to the booking calendar. Page works on all 3 locales with translated labels.
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `curl -X POST http://localhost:3000/api/analyze -H 'Content-Type: application/json' -d '{"url":"https://example.com"}'` returns 200 with scores object containing 5 categories
- `curl -X POST http://localhost:3000/api/analyze -H 'Content-Type: application/json' -d '{"url":"not-a-url"}'` returns 422
- `curl -X POST http://localhost:3000/api/analyze -d 'invalid'` returns 400
- /de/analyse page renders with form, /en/analyse renders with English labels
</verification>

<success_criteria>
- POST /api/analyze validates input and returns mock scores for 5 categories within 2 seconds
- Invalid requests return appropriate 400/422 error responses
- /de/analyse page shows URL input form and displays animated score gauges after submission
- Score gauges animate from 0 to final value with CSS transitions
- Page works on all 3 locales with translated labels
- No CORS headers expose the endpoint to cross-origin requests
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-page/03-02-SUMMARY.md`
</output>
