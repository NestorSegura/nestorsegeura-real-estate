---
phase: 03-landing-page
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - scripts/seed-content.ts
  - src/app/[locale]/analyse/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visiting /de renders all 8 block sections (hero, features, problem/solution, services, testimonials, references, FAQ, CTA) with German content from Sanity"
    - "Visiting / and /es renders corresponding locale pages with translated content"
    - "Every screen section contains exactly one appointment CTA linking to external calendar"
    - "SiteSettings document has navigation links matching the section IDs from Plan 01"
    - "Layout is usable on 375px viewport without horizontal overflow"
  artifacts:
    - path: "scripts/seed-content.ts"
      provides: "Sanity content migration script for all 3 locales + siteSettings"
      min_lines: 100
  key_links:
    - from: "scripts/seed-content.ts"
      to: "sanity"
      via: "Sanity client createOrReplace mutations"
      pattern: "createOrReplace|createIfNotExists"
    - from: "Sanity siteSettings.navigation[].href"
      to: "block section IDs"
      via: "anchor links matching section id attributes"
      pattern: "#leistungen|#referenzen|#faq|#kontakt"
---

<objective>
Populate Sanity with German, English, and Spanish homepage content for all 8 block sections, configure siteSettings with navigation links and CTA URL, then verify the full landing page renders responsively.

Purpose: The block components and navbar exist but have no content to render. This plan creates the actual conversion page content in Sanity and verifies the complete user experience end-to-end.

Output: Sanity migration script that seeds all content, populated CMS, verified responsive layout.
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-landing-page/03-RESEARCH.md
@.planning/phases/03-landing-page/03-01-SUMMARY.md
@.planning/phases/03-landing-page/03-02-SUMMARY.md

@src/sanity/lib/queries.ts
@src/sanity/schemas/documents/siteSettings.ts
@src/blocks/PageBuilder.tsx
@src/i18n/messages/de.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sanity content migration script for all locales and siteSettings</name>
  <files>scripts/seed-content.ts</files>
  <action>
    Create `scripts/seed-content.ts` that uses the Sanity client to seed content.

    **Script setup:**
    - Import `createClient` from `@sanity/client`
    - Read `NEXT_PUBLIC_SANITY_PROJECT_ID` and `NEXT_PUBLIC_SANITY_DATASET` from process.env (dotenv or inline)
    - Use a `SANITY_API_TOKEN` env var for write access (the user may need to create a write token in Sanity dashboard if not existing)
    - Use `apiVersion: '2024-01-01'`
    - Add usage instructions in a comment at top of file

    **SiteSettings document:**
    ```
    {
      _id: 'siteSettings',
      _type: 'siteSettings',
      siteName: 'nestorsegura.com',
      tagline: 'Web Design fuer Immobilienmakler',
      defaultCtaHref: 'https://cal.com/nestorsegura/erstgespraech',
      navigation: [
        { _key: 'nav-1', label: 'Leistungen', href: '#leistungen' },
        { _key: 'nav-2', label: 'Projekte', href: '#projekte' },
        { _key: 'nav-3', label: 'Referenzen', href: '#referenzen' },
        { _key: 'nav-4', label: 'Analyse', href: '/analyse' },
        { _key: 'nav-5', label: 'FAQ', href: '#faq' },
        { _key: 'nav-6', label: 'Kontakt', href: '#kontakt' },
      ],
      footer: {
        socialLinks: [
          { _key: 'social-1', platform: 'LinkedIn', url: 'https://linkedin.com/in/nestorsegura' },
          { _key: 'social-2', platform: 'Instagram', url: 'https://instagram.com/nestorsegura' },
        ]
      }
    }
    ```

    **German homepage (primary conversion page):**
    Create a Page document with `_id: 'page-home-de'`, `slug: { current: 'home' }`, `language: 'de'`, and `sections` array containing all 8 blocks. Draft the German copy with this tone: Professional but warm, Sie-form, conversational, approachable expert (per locked decision).

    The sections array must include these blocks in order:
    1. `heroSection`: headline "Ihre Website arbeitet nicht fuer Sie? Das aendern wir.", subheadline about converting visitors to clients for real estate agents, ctaLabel "Erstgespraech vereinbaren", ctaHref from siteSettings (use the Cal.com placeholder URL), variant "svgPath", colorScheme "dark"
    2. `featureStrip`: 3 features (Branchenkenntnis, Conversion-Optimierung, Persoenliche Betreuung), colorScheme "light"
    3. `problemSolutionBlock`: title "Kennen Sie das?", 3 problems real estate agents face (no leads from website, losing clients to competition, no time for marketing), then solutions. colorScheme "light"
    4. `servicesBlock`: title "Unsere Leistungen", 3 services (Website-Erstellung, Website-Analyse, Laufende Betreuung) with feature bullets. Each service card gets its own ctaLabel "Erstgespraech vereinbaren" and ctaHref. colorScheme "dark"
    5. `testimonialsBlock`: title "Das sagen unsere Kunden", 3 fictional testimonials from German real estate agents (use realistic German names, company names, and quotes about results). colorScheme "light"
    6. `referencesBlock`: title "Unsere Projekte", 3 reference entries with fictional agency names and descriptions. colorScheme "light"
    7. `faqBlock`: title "Haeufig gestellte Fragen", 5-6 FAQs about pricing, timeline, CMS, mobile, SEO, ongoing support. colorScheme "light"
    8. `ctaBlock`: headline "Bereit fuer mehr Anfragen?", subtext about free consultation, ctaLabel "Erstgespraech vereinbaren", ctaHref from siteSettings, variant "primary", colorScheme "dark"

    All blocks: `enabled: true`, appropriate `spacing` values (first block "spacious", middle blocks "normal", last block "normal").

    **Every section that can have a CTA must have exactly one CTA linking to the external calendar** (per LAND-04 requirement). The CTA copy is always "Erstgespraech vereinbaren" (per locked decision). Blocks that support ctaHref/ctaLabel: heroSection, servicesBlock (per service), ctaBlock. Other blocks don't have CTA fields.

    **English homepage:**
    Create a Page document with `_id: 'page-home-en'`, `slug: { current: 'home' }`, `language: 'en'`. Same structure as German but with English copy. Professional warm tone. CTA: "Book Free Consultation".

    **Spanish homepage:**
    Create a Page document with `_id: 'page-home-es'`, `slug: { current: 'home' }`, `language: 'es'`. Same structure with Spanish copy. CTA: "Agendar consulta gratuita".

    **Script execution:**
    - Use `client.createOrReplace()` for each document (idempotent)
    - Run all mutations in a transaction or sequentially
    - Log progress: "Creating siteSettings...", "Creating DE homepage...", etc.
    - Add to package.json scripts: `"seed": "npx tsx scripts/seed-content.ts"` (or provide run instructions in comments)

    **Important notes:**
    - Each block in the sections array needs a unique `_key` (use descriptive keys like `hero-1`, `features-1`, etc.)
    - Each item within arrays (features, faqs, testimonials) also needs `_key` values
    - The `_type` for each block must match exactly what the Sanity schemas define (heroSection, featureStrip, problemSolutionBlock, servicesBlock, testimonialsBlock, referencesBlock, faqBlock, ctaBlock)
    - Testimonial avatars: omit image field (existing component renders initials placeholder per [02-03] decision)
    - Use proper German umlauts in the actual content (ae -> a with umlaut, oe -> o with umlaut, ue -> u with umlaut) -- the ASCII versions above are just for this plan file encoding
  </action>
  <verify>
    `npx tsc --noEmit` passes on the seed script (or `npx tsx --dry-run scripts/seed-content.ts` if supported). `npm run build` completes without errors. File exists at `scripts/seed-content.ts` and contains `createOrReplace` calls for siteSettings, page-home-de, page-home-en, and page-home-es documents. Navigation array contains only valid section anchors: `#leistungen`, `#projekte`, `#referenzen`, `#faq`, `#kontakt` (no `#about`).
  </verify>
  <done>
    Sanity CMS contains siteSettings (nav links, CTA URL, social links) and 3 homepage Page documents (DE, EN, ES) each with all 8 block sections populated with professional copy. The seed script is idempotent and can be re-run safely.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete German landing page with:
    - Sticky navbar with section links, CTA button, language switcher, mobile drawer
    - All 8 content sections rendered from Sanity (hero through CTA)
    - Each section with exactly one booking CTA ("Erstgespraech vereinbaren")
    - Website analysis tool at /de/analyse with URL form and score gauges
    - English (/en) and Spanish (/es) pages with translated content
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 (German homepage) -- verify all 8 sections render with German copy
    2. Scroll down -- verify navbar becomes sticky with blur effect
    3. Click each navbar anchor link -- verify smooth scroll to correct section
    4. Click "Erstgespraech vereinbaren" CTA -- verify it opens in new tab
    5. Click "EN" in language switcher -- verify English page renders at /en
    6. Click "ES" -- verify Spanish page at /es
    7. Resize browser to 375px width -- verify no horizontal overflow, layout is usable
    8. On mobile width, tap hamburger -- verify drawer slides in from right with all links
    9. Visit /de/analyse -- enter a URL and submit -- verify score gauges animate
    10. Review German copy tone -- should feel professional, warm, Sie-form, approachable expert
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 8 block sections render on /de with German content from Sanity
- / and /es render with English and Spanish content respectively
- Every CTA section links to external calendar (opens new tab)
- /de/analyse form returns mock scores and displays gauges
- 375px viewport has no horizontal overflow
- Navbar renders on all pages with logo, links, CTA, language switcher
</verification>

<success_criteria>
- Phase 3 success criteria 1: /de renders all 8 block sections with German content from Sanity
- Phase 3 success criteria 2: / and /es render with translated content
- Phase 3 success criteria 3: Every section has one appointment CTA linking to external calendar
- Phase 3 success criteria 4: Website analysis form returns mock scores within 2 seconds
- Phase 3 success criteria 5: Layout usable on 375px viewport
- Phase 3 success criteria 6: Navbar renders with logo, menu items, CTA on all locales
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-page/03-03-SUMMARY.md`
</output>
