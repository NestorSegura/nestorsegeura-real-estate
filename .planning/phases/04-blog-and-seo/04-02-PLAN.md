---
phase: 04-blog-and-seo
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/[locale]/blog/page.tsx
  - src/app/[locale]/blog/[slug]/page.tsx
  - src/components/blog/BlogListing.tsx
  - src/components/blog/PostCard.tsx
  - src/components/blog/FeaturedPostCard.tsx
  - src/components/blog/PortableTextRenderer.tsx
  - src/components/blog/TableOfContents.tsx
  - src/components/blog/AuthorCard.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /blog shows a featured post hero-sized at top and remaining posts in a card grid"
    - "Post cards show featured image, title, excerpt, date, and estimated reading time"
    - "Visiting /blog/[slug] renders the full post with portable text correctly formatted"
    - "Blog post page has a sticky sidebar with table of contents, appointment CTA, and author card"
    - "Table of contents links scroll to the correct heading in the article"
    - "Blog pages work for all three locales (de at /blog, en at /en/blog, es at /es/blog)"
  artifacts:
    - path: "src/app/[locale]/blog/page.tsx"
      provides: "Blog listing route"
      contains: "sanityFetch"
    - path: "src/app/[locale]/blog/[slug]/page.tsx"
      provides: "Blog post detail route"
      contains: "PortableTextRenderer"
    - path: "src/components/blog/PortableTextRenderer.tsx"
      provides: "Portable text rendering with custom block components"
      contains: "PortableText"
    - path: "src/components/blog/TableOfContents.tsx"
      provides: "Auto-generated sticky TOC from headings"
      contains: "use client"
  key_links:
    - from: "src/app/[locale]/blog/page.tsx"
      to: "@/sanity/lib/live"
      via: "sanityFetch with ALL_POSTS_QUERY"
      pattern: "ALL_POSTS_QUERY"
    - from: "src/app/[locale]/blog/[slug]/page.tsx"
      to: "@/sanity/lib/live"
      via: "sanityFetch with POST_BY_SLUG_QUERY"
      pattern: "POST_BY_SLUG_QUERY"
    - from: "src/components/blog/PortableTextRenderer.tsx"
      to: "src/components/blog/TableOfContents.tsx"
      via: "matching heading IDs (h2-{_key} / h3-{_key} format)"
      pattern: "id=.*_key"
---

<objective>
Build the blog listing page (featured + grid layout) and blog post detail page (article + sidebar with TOC, CTA, author card) using Sanity data and portable text rendering.

Purpose: These are the core blog routes that make blog content accessible and readable, fulfilling BLOG-01, BLOG-02, BLOG-03.
Output: Two route pages and supporting components for blog listing and post reading experience.
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blog-and-seo/04-RESEARCH.md
@.planning/phases/04-blog-and-seo/04-01-SUMMARY.md

@src/app/[locale]/layout.tsx
@src/app/[locale]/page.tsx
@src/sanity/lib/queries.ts
@src/sanity/lib/image.ts
@src/lib/blog.ts
@src/i18n/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build blog listing page with featured post and card grid</name>
  <files>
    src/app/[locale]/blog/page.tsx
    src/components/blog/BlogListing.tsx
    src/components/blog/PostCard.tsx
    src/components/blog/FeaturedPostCard.tsx
  </files>
  <action>
1. Create `src/app/[locale]/blog/page.tsx`:
   - Server Component. Import `sanityFetch` from `@/sanity/lib/live`, `ALL_POSTS_QUERY` from `@/sanity/lib/queries`
   - Accept `params: Promise<{ locale: string }>`, await it, call `setRequestLocale(locale)` before any async work
   - Validate locale with `hasLocale(routing.locales, locale)`, call `notFound()` if invalid
   - Fetch posts: `sanityFetch({ query: ALL_POSTS_QUERY, params: { language: locale } })`
   - Destructure: `const [featuredPost, ...restPosts] = data ?? []`
   - Render `<BlogListing>` passing featuredPost and restPosts
   - Handle empty state: if no posts, show a message like "Noch keine Beitrage" (localize later if needed)

2. Create `src/components/blog/FeaturedPostCard.tsx`:
   - Server Component. Takes the featured post data as props
   - Render hero-sized: large image (using `urlFor` from `@/sanity/lib/image` for the mainImage), title as h2, excerpt, date formatted with `toLocaleDateString('de-DE')`, estimated reading time (e.g., "3 Min. Lesezeit"), and a "Weiterlesen" link
   - Use `Link` from `@/i18n/navigation` for the post link: `href={'/blog/' + post.slug.current}`
   - Tailwind: full-width card, image aspect-video, padding, hover state on the link

3. Create `src/components/blog/PostCard.tsx`:
   - Server Component. Takes a post object as props
   - Show: mainImage (via urlFor, aspect-video, rounded), title, excerpt (truncated to 2 lines with line-clamp-2), date, estimated reading time, category badge if present
   - Use `Link` from `@/i18n/navigation` for the post link
   - Tailwind: card with shadow-sm, hover:shadow-md transition, rounded-lg, overflow-hidden

4. Create `src/components/blog/BlogListing.tsx`:
   - Server Component. Accepts `featuredPost` and `posts` props
   - Layout: FeaturedPostCard at top (full width), then a responsive grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` for remaining PostCards
   - Section padding and max-w-6xl mx-auto container
  </action>
  <verify>
    - `npx tsc --noEmit` shows no errors in blog files
    - Blog listing page file exists at `src/app/[locale]/blog/page.tsx`
    - All components use `Link` from `@/i18n/navigation` (not `next/link`)
    - `grep -r "next/link" src/components/blog/` returns nothing
  </verify>
  <done>Blog listing page renders featured post hero + card grid with images, titles, excerpts, dates, reading times. Links navigate to individual post pages using locale-aware routing.</done>
</task>

<task type="auto">
  <name>Task 2: Build blog post detail page with portable text, TOC sidebar, and author card</name>
  <files>
    src/app/[locale]/blog/[slug]/page.tsx
    src/components/blog/PortableTextRenderer.tsx
    src/components/blog/TableOfContents.tsx
    src/components/blog/AuthorCard.tsx
  </files>
  <action>
1. Create `src/components/blog/PortableTextRenderer.tsx`:
   - Import `PortableText` and `PortableTextComponents` from `@portabletext/react`
   - Import `Image` from `next/image`, `urlFor` from `@/sanity/lib/image`
   - Define custom components:
     - `types.image`: render `<figure>` with `<Image>` (urlFor for src, width 800, height 450, rounded-lg), optional `<figcaption>` from value.caption
     - `block.h2`: render `<h2>` with `id={`h2-${props.value._key}`}` class="text-2xl font-bold mt-10 mb-4 scroll-mt-24" — the scroll-mt-24 ensures the heading isn't hidden under sticky navbar when scrolled to
     - `block.h3`: render `<h3>` with `id={`h3-${props.value._key}`}` class="text-xl font-semibold mt-8 mb-3 scroll-mt-24"
     - `block.blockquote`: border-l-4 border-primary pl-4 italic my-6
     - `marks.link`: `<a>` with href, target="_blank", rel="noopener noreferrer", underline styling
   - Wrap in `<div className="prose prose-lg max-w-none">` — if Tailwind prose plugin not available, use manual typography classes instead (check if @tailwindcss/typography is installed; if not, skip prose class and apply manual spacing/font-size classes)
   - Export as named export `PortableTextRenderer`

2. Create `src/components/blog/TableOfContents.tsx`:
   - Client Component (`'use client'`)
   - Accept `headings: TocItem[]` prop (import TocItem from `@/lib/blog`)
   - Render a `<nav>` with heading "Inhalt" and a list of anchor links
   - Each link: `<a href={`#${heading.id}`}>` with text, h3 items indented (pl-3)
   - Add active heading tracking: use IntersectionObserver to observe all heading elements by ID, highlight the currently visible heading in the TOC list
   - Smooth scroll: links use `onClick` with `document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })` and `e.preventDefault()`
   - Styling: sticky top-24, border-l-2, compact text-sm, active item gets text-primary + font-medium

3. Create `src/components/blog/AuthorCard.tsx`:
   - Server Component. Accept author object with name, bio, image
   - Render: small avatar image (urlFor, 64x64, rounded-full), name as bold text, bio as text-sm text-muted-foreground
   - Wrapped in a card with border and padding

4. Create `src/app/[locale]/blog/[slug]/page.tsx`:
   - Server Component. Accept `params: Promise<{ locale: string; slug: string }>`
   - Await params, call `setRequestLocale(locale)`, validate locale
   - Fetch: `sanityFetch({ query: POST_BY_SLUG_QUERY, params: { slug, language: locale } })`
   - If no post, call `notFound()`
   - Extract headings: `extractHeadings(post.body ?? [])` from `@/lib/blog`
   - Layout: `<article>` with grid `grid-cols-1 lg:grid-cols-[1fr_300px] gap-12 max-w-5xl mx-auto px-4 py-12`
     - Left column: post header (title as h1, date + reading time + category badge, mainImage), then `<PortableTextRenderer value={post.body ?? []} />`
     - Right column (`<aside>` with `lg:sticky lg:top-24 lg:self-start space-y-6`): `<TableOfContents headings={headings} />`, appointment CTA block (link to Cal.com `https://cal.com/nestorsegura/erstgespraech` styled as a card with button), `<AuthorCard>` if author exists
   - Add `generateStaticParams` that fetches `ALL_POSTS_FOR_SITEMAP_QUERY` and maps to `{ locale: post.language, slug: post.slug.current }`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Blog post page exists at `src/app/[locale]/blog/[slug]/page.tsx`
    - PortableTextRenderer renders h2/h3 with matching ID format `h2-{_key}` / `h3-{_key}`
    - `grep -q "scroll-mt" src/components/blog/PortableTextRenderer.tsx` confirms scroll margin on headings
    - TableOfContents is a client component: `grep -q "use client" src/components/blog/TableOfContents.tsx`
  </verify>
  <done>Blog post page renders article content via portable text with custom block/mark renderers, sticky sidebar with auto-generated TOC (active heading tracking + smooth scroll), appointment CTA linking to Cal.com, and author card. Heading IDs match between PortableTextRenderer and TableOfContents.</done>
</task>

</tasks>

<verification>
- `npm run build` or `npx tsc --noEmit` passes without errors in blog files
- Blog listing page fetches from Sanity with ALL_POSTS_QUERY
- Blog post page fetches from Sanity with POST_BY_SLUG_QUERY
- All links use `@/i18n/navigation` Link, not `next/link`
- Heading IDs use consistent `{style}-{_key}` format across PortableTextRenderer and extractHeadings
- `setRequestLocale(locale)` called in both blog pages before async work
</verification>

<success_criteria>
- /[locale]/blog route renders featured + grid layout with post data from Sanity
- /[locale]/blog/[slug] route renders full article with portable text, TOC, CTA, author card
- Post cards show image, title, excerpt, date, reading time
- TOC auto-generates from headings and tracks active heading on scroll
- Sidebar CTA links to Cal.com appointment URL
</success_criteria>

<output>
After completion, create `.planning/phases/04-blog-and-seo/04-02-SUMMARY.md`
</output>
