---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - next.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/theme-provider.tsx
  - src/blocks/.gitkeep
  - src/sanity/.gitkeep
  - src/types/index.ts
  - components.json
autonomous: true

must_haves:
  truths:
    - "npm run dev starts without errors on a fresh clone"
    - "Tailwind utility classes produce styled output (not unstyled)"
    - "Both light and dark mode tokens are defined with correct brand colors"
    - "shadcn/ui Button component renders and responds to theme"
    - "src/ directory follows the agreed layer-based layout with blocks/ as first-class"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand OKLCH color tokens for light and dark mode"
      contains: "--primary: oklch"
    - path: "src/components/theme-provider.tsx"
      provides: "next-themes ThemeProvider wrapper for system-preference dark mode"
      contains: "NextThemesProvider"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
      contains: "buttonVariants"
    - path: "src/lib/utils.ts"
      provides: "clsx + tailwind-merge utility"
      contains: "twMerge"
    - path: "package.json"
      provides: "All Phase 1 dependencies installed"
      contains: "next-intl"
  key_links:
    - from: "src/app/globals.css"
      to: "Tailwind v4 engine"
      via: "@import tailwindcss and @theme inline"
      pattern: "@import.*tailwindcss"
    - from: "src/components/theme-provider.tsx"
      to: "next-themes"
      via: "ThemeProvider with attribute=class"
      pattern: "attribute.*class"
---

<objective>
Scaffold the Next.js 15 project with TypeScript strict mode, Tailwind v4, shadcn/ui, and the agreed directory layout. Configure brand color tokens (OKLCH) for both light and dark modes, and install all Phase 1 dependencies upfront.

Purpose: Every subsequent plan in this phase (and every future phase) depends on a working, correctly configured project skeleton. This plan creates that foundation.
Output: A running Next.js 15 dev server with Tailwind v4 styling, shadcn/ui initialized, dark/light mode tokens, and the full directory skeleton.
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 and install all Phase 1 dependencies</name>
  <files>
    package.json
    tsconfig.json
    postcss.config.mjs
    next.config.ts
    src/app/layout.tsx
    src/app/page.tsx
  </files>
  <action>
    1. Scaffold the project in the current directory (it is empty except for .planning/, .git/, .idea/):
       ```bash
       npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir
       ```
       Accept all defaults. This creates the base project with Tailwind v4 and PostCSS correctly configured.

    2. Install all Phase 1 dependencies in one command:
       ```bash
       npm install next-intl@^4 sanity@^5 next-sanity@^12 next-themes
       ```
       IMPORTANT: `sanity@^5` is correct (npm package v5 = Sanity Studio v3 generation). Do NOT install `sanity@3`.

    3. Initialize shadcn/ui:
       ```bash
       npx shadcn@latest init -d
       ```
       The `-d` flag uses defaults (TypeScript, App Router style). This installs tw-animate-css, clsx, tailwind-merge, lucide-react and creates components.json and src/lib/utils.ts.

    4. Add the Button component:
       ```bash
       npx shadcn@latest add button -y
       ```

    5. Verify tsconfig.json has `"strict": true` (create-next-app@15 sets this by default). If not, set it.

    6. Verify postcss.config.mjs contains `@tailwindcss/postcss` plugin. create-next-app@15 --tailwind handles this.
  </action>
  <verify>
    Run `npm run dev` — the Next.js dev server starts without errors.
    Run `npx tsc --noEmit` — no TypeScript errors.
    Check `package.json` contains: next, react, next-intl, sanity, next-sanity, next-themes, tw-animate-css.
  </verify>
  <done>
    All Phase 1 npm dependencies installed. Dev server starts. TypeScript strict mode enabled. shadcn/ui initialized with Button component available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure brand tokens, ThemeProvider, and directory skeleton</name>
  <files>
    src/app/globals.css
    src/components/theme-provider.tsx
    src/blocks/.gitkeep
    src/sanity/.gitkeep
    src/types/index.ts
    src/i18n/.gitkeep
  </files>
  <action>
    1. Replace `src/app/globals.css` with the brand-themed version. Use the exact OKLCH values from RESEARCH.md Pattern 8:
       - Keep `@import "tailwindcss"` and `@import "tw-animate-css"` at top
       - Add `@custom-variant dark (&:is(.dark *));`
       - `:root` block with light mode tokens:
         - `--background: oklch(0.97 0.003 80)` (#EFEEEC off-white — per user decision)
         - `--foreground: oklch(0.12 0 0)` (#131313 — per user decision)
         - `--primary: oklch(0.45 0.18 290)` (jibemates purple approximate — per user decision)
         - `--primary-foreground: oklch(0.97 0.003 80)`
         - Include all shadcn token slots: secondary, muted, accent, border, input, ring, destructive, card, popover, sidebar tokens, chart colors, radius
       - `.dark` block with dark mode tokens:
         - `--background: oklch(0.12 0 0)` (#131313 — per user decision)
         - `--foreground: oklch(0.95 0.003 80)` (near-white with warmth)
         - `--primary: oklch(0.72 0.14 290)` (lighter purple for dark mode contrast)
         - All corresponding shadcn dark tokens
       - `@theme inline` block mapping all CSS vars to Tailwind color utilities (--color-background, --color-foreground, --color-primary, etc.)
       - Add base body styles: `body { background: var(--background); color: var(--foreground); }` using `@layer base`

    2. Create `src/components/theme-provider.tsx` (from RESEARCH.md Pattern 6):
       ```typescript
       'use client'
       import { ThemeProvider as NextThemesProvider } from 'next-themes'
       export function ThemeProvider({ children, ...props }: React.ComponentProps<typeof NextThemesProvider>) {
         return <NextThemesProvider {...props}>{children}</NextThemesProvider>
       }
       ```

    3. Update `src/app/layout.tsx` to wrap children with ThemeProvider:
       - Import ThemeProvider from `@/components/theme-provider`
       - Add `suppressHydrationWarning` to `<html>` tag
       - Wrap `{children}` with `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>`
       - Keep the existing font setup from create-next-app

    4. Update `src/app/page.tsx` to a minimal placeholder that uses the Button component and shows the brand is working:
       - Import Button from `@/components/ui/button`
       - Render a centered layout with heading "nestorsegura.com" and a Button with text "Get Started"
       - Use `bg-background text-foreground` classes to verify token wiring

    5. Create the directory skeleton (empty directories with .gitkeep where no files exist yet):
       - `src/blocks/.gitkeep` (block components — per user decision, first-class separate from components)
       - `src/sanity/schemas/.gitkeep` (Sanity schemas — Claude's discretion: collocated in src/)
       - `src/sanity/lib/.gitkeep` (Sanity client utilities)
       - `src/types/index.ts` (empty file, will hold type augmentation in plan 01-02)
       - `src/i18n/messages/.gitkeep` (translation files added in plan 01-02)

    6. Ensure all files use kebab-case naming per user decision (theme-provider.tsx, not ThemeProvider.tsx).
  </action>
  <verify>
    Run `npm run dev` — page renders with off-white background in light mode.
    Run `npx tsc --noEmit` — no TypeScript errors.
    Verify `src/blocks/`, `src/sanity/schemas/`, `src/sanity/lib/`, `src/types/`, `src/i18n/messages/` directories exist.
    Verify globals.css contains `--primary: oklch(0.45 0.18 290)` and `.dark` block.
  </verify>
  <done>
    Brand color tokens (OKLCH) configured for both light and dark modes in globals.css. ThemeProvider wraps the app with system-preference dark mode. Directory skeleton matches the agreed layout with src/blocks/ as first-class. Placeholder page renders styled with brand colors.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Visiting `http://localhost:3000` shows a styled page with off-white background and purple accent button
3. `npx tsc --noEmit` passes with zero errors
4. `src/blocks/`, `src/sanity/`, `src/types/`, `src/i18n/` directories exist
5. `globals.css` has both `:root` and `.dark` color token blocks with OKLCH values
6. ThemeProvider is wired in layout.tsx with `defaultTheme="system"`
</verification>

<success_criteria>
- Dev server runs without errors
- Brand colors visible in browser (off-white background, purple accent on button)
- All Phase 1 dependencies in package.json
- TypeScript strict mode active (tsconfig.json strict: true)
- Directory layout matches user decision (src/blocks/ separate from src/components/)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
