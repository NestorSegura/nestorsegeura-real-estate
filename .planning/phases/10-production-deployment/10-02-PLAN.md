---
phase: 10-production-deployment
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/smoke-test.sh
autonomous: false
user_setup:
  - service: cloudflare-deploy-hook
    why: "Create Deploy Hook for Workers Builds so Sanity webhook can trigger rebuilds"
    dashboard_config:
      - task: "Create Deploy Hook (branch: main, name: Sanity CMS)"
        location: "Workers & Pages → realestatestrategy-eu → Settings → Builds → Deploy Hooks"
  - service: sanity-webhook
    why: "Fire CF Deploy Hook on content create/update/delete (all document types)"
    dashboard_config:
      - task: "Add webhook pointing at CF Deploy Hook URL"
        location: "sanity.io/manage → Project → API → Webhooks → Add webhook"
  - service: cloudflare-html-rewriting-audit
    why: "Verify no CF feature mutates Worker-served HTML"
    dashboard_config:
      - task: "Verify Rocket Loader is OFF (Speed → Optimization → Content Optimization)"
        location: "CF Dashboard → [Zone] → Speed → Optimization → Content Optimization"

must_haves:
  truths:
    - "Publishing/editing/deleting any Sanity document triggers a CF rebuild within 60 seconds of save"
    - "A fresh production deployment reflects the Sanity change within ~3 minutes total (build + deploy)"
    - "Worker-served HTML is byte-identical to `astro build` output (no minification, no script-tag rewriting, no email obfuscation mutation)"
    - "All 3 locale homepages (/, /en, /es), sitemap.xml, robots.txt, og-default.png return 200 from production"
    - "Lighthouse mobile Performance score ≥ 90 on the production homepage"
  artifacts:
    - path: "scripts/smoke-test.sh"
      provides: "Executable bash script that curls all critical URLs and exits non-zero on any failure"
      min_lines: 30
    - path: "CF Deploy Hook (Worker: realestatestrategy-eu, branch: main)"
      provides: "Authenticated POST endpoint to trigger rebuilds"
    - path: "Sanity webhook 'Cloudflare Rebuild'"
      provides: "Fires Deploy Hook on all mutation events across all document types, dataset=production"
  key_links:
    - from: "Sanity Studio publish action"
      to: "CF Deploy Hook URL"
      via: "Sanity webhook POST with no auth header"
      pattern: "api.cloudflare.com/client/v4/workers/builds/deploy_hooks"
    - from: "CF Deploy Hook trigger"
      to: "Workers Build on main branch"
      via: "CF-internal queue, dedup concurrent requests, 10 builds/min rate limit"
    - from: "scripts/smoke-test.sh"
      to: "production URLs (3 locales + sitemap + robots + og-default)"
      via: "curl with HTTP status assertion"
---

<objective>
Wire Sanity → Cloudflare rebuild webhook, audit CF HTML-rewriting features (close INFR-08 Auto Minify requirement as "verified not applicable" per RESEARCH.md finding that CF removed the feature Aug 5, 2024), write the smoke-test script Claude owns, and run the launch gate (automated smoke + Lighthouse + Nestor's manual walkthrough).

Purpose: Flip the site from "deployed" to "launched" with live content editing + a verified HTML-integrity baseline. Satisfies INFR-06, INFR-07, INFR-08 (the last as a documented no-op).

Output: Sanity edits rebuild the site autonomously; a checked-in smoke script; documented evidence that CF is not mutating HTML; green launch-gate checklist.
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-deployment/10-CONTEXT.md
@.planning/phases/10-production-deployment/10-RESEARCH.md
@.planning/phases/10-production-deployment/10-01-SUMMARY.md
@src/pages/sitemap-index.xml.ts
@public/robots.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/smoke-test.sh (automated launch-gate check)</name>
  <files>scripts/smoke-test.sh</files>
  <action>
    Create `scripts/smoke-test.sh` as an executable bash script that runs curl-based HTTP status checks against a base URL (passed as $1). This is the automated half of the launch gate from CONTEXT.md.

    Use the template in 10-RESEARCH.md "Smoke Test Shell Script" section verbatim as the starting point, with these REQUIRED checks:

    Locale homepages (all 3):
    - GET `$BASE_URL/` → 200 (DE homepage, no prefix)
    - GET `$BASE_URL/en` → 200 OR 301 (CF may add trailing slash); if 3xx, follow and assert final 200
    - GET `$BASE_URL/es` → 200 OR 301 (same)

    Static/generated assets:
    - GET `$BASE_URL/sitemap-index.xml` → 200 (verify exact path — check `src/pages/sitemap-index.xml.ts` or `astro.config.mjs` sitemap output; adjust if it's `/sitemap.xml` instead)
    - GET `$BASE_URL/robots.txt` → 200
    - GET `$BASE_URL/og-default.png` → 200

    Redirect (only when BASE_URL is the apex domain, not a preview URL):
    - If `$BASE_URL` contains `realestatestrategy.eu` (not workers.dev): GET `https://www.realestatestrategy.eu/` → expect 301 with `Location: https://realestatestrategy.eu/`
    - Skip this check for preview URLs (the www redirect rule only applies to the apex zone)

    Content sanity (at least one):
    - GET `$BASE_URL/` and grep the response body for the siteSettings tagline "Web Design für Immobilienmakler" OR for a known h1/hero word — exit non-zero if absent (catches "blank page" regressions)

    Requirements for the script:
    - Shebang `#!/usr/bin/env bash`
    - `set -euo pipefail` at top
    - Usage message: `Usage: $0 <base-url>` if $1 missing
    - A `check()` function that takes (url, expected_status, description)
    - A final summary line `Results: X passed, Y failed` and `exit 0` only if all pass
    - `chmod +x` the file after writing (Claude: use `chmod +x scripts/smoke-test.sh` after Write)
    - Max timeout per request: 15s (`--max-time 15`)

    Before committing, RUN the script against the current CF preview URL (Claude asks human for preview URL from 10-01-SUMMARY or CF dashboard) to ensure the script itself works. Iterate if any check is wrong (e.g., sitemap path mismatch).

    Commit: `feat(10): add scripts/smoke-test.sh for launch-gate verification`

    Note: Lighthouse mobile check is NOT included in this script — it's run separately in Task 3 via `npx lhci` to avoid bundling the tool requirement into every smoke run.
  </action>
  <verify>
    `bash scripts/smoke-test.sh https://realestatestrategy.eu` exits 0 with all PASS lines after 10-01 is live.
    `bash scripts/smoke-test.sh` with no args prints usage and exits non-zero.
    `[ -x scripts/smoke-test.sh ]` returns true.
  </verify>
  <done>
    scripts/smoke-test.sh exists, is executable, contains all 6 required checks (3 locales + sitemap + robots + og + content sanity), and the www→apex redirect check is gated on the base URL being the apex domain. Committed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HUMAN — Create CF Deploy Hook + wire Sanity webhook + audit CF HTML features</name>
  <what-human-does>
    Three dashboard jobs. Claude cannot do any of this via CLI.

    **Step 2a: Create Cloudflare Deploy Hook**
    1. Cloudflare Dashboard → Workers & Pages → realestatestrategy-eu → Settings → Builds → Deploy Hooks
       (This section exists because Deploy Hooks for Workers Builds launched April 1, 2026 — Pitfall 5.)
    2. Click "Add deploy hook"
    3. Name: `Sanity CMS`
    4. Branch: `main`
    5. Create → copy the URL (format: `https://api.cloudflare.com/client/v4/workers/builds/deploy_hooks/<ID>`)
    6. Test it before moving on: in a terminal run
       `curl -X POST "<paste-url>"`
       then check the CF dashboard Deployments tab — a new build should appear within ~10 seconds. Wait for it to go green.

    **Step 2b: Add Sanity webhook**
    1. https://sanity.io/manage → select the project → API tab → Webhooks → Create webhook (GROQ-powered webhook)
    2. Name: `Cloudflare Rebuild`
    3. Description: `Triggers CF Workers Build on any content mutation`
    4. Dataset: `production`
    5. Trigger on: check ALL three — Create, Update, Delete (per CONTEXT.md locked decision: "all mutation events")
    6. Filter: leave EMPTY (per CONTEXT.md: all document types)
    7. Projection: leave EMPTY (per CONTEXT.md: no debounce, no projection filter)
    8. URL: paste the CF Deploy Hook URL from Step 2a
    9. HTTP method: POST
    10. HTTP Headers: none needed (Deploy Hook URL is self-authenticating — don't add an Authorization header, it'd be ignored anyway)
    11. API version: default (v2021-03-25 or newer, doesn't matter for trigger-only webhooks)
    12. Enable webhook → Save
    13. Test: open any Sanity document in the Studio, change one field, publish. Within ~10–30 seconds, check CF Deployments tab — a new build should appear, tagged with Deploy Hook as the trigger source. Wait for it to go green.

    **Step 2c: Audit CF HTML-rewriting features (satisfies INFR-08)**
    Per 10-RESEARCH.md "CF HTML-Rewriting Features Audit":

    1. **Auto Minify HTML (INFR-08 — THE KEY ONE)**
       - Navigate: CF Dashboard → [Zone: realestatestrategy.eu] → Speed → Optimization → Content Optimization
       - Look for any "Auto Minify" toggle for HTML.
       - EXPECTED OUTCOME: the toggle does NOT exist. CF removed Auto Minify on August 5, 2024.
       - Record as evidence: take a screenshot of the Content Optimization page showing NO Auto Minify HTML option. This screenshot IS the verification artifact for INFR-08 — close the requirement with the note "verified not applicable: CF removed Auto Minify HTML on 2024-08-05, no toggle exists."
       - If (unexpectedly) the toggle exists: turn it OFF.

    2. **Rocket Loader**
       - Same page (Speed → Optimization → Content Optimization).
       - Confirm Rocket Loader is OFF. If it's ON, turn it OFF (it rewrites script tags; even though Workers responses are reportedly exempt, the default-off posture is safer per RESEARCH.md).

    3. **Email Obfuscation**
       - CF Dashboard → [Zone] → Scrape Shield → Email Address Obfuscation
       - Leave at default (ON is fine) — RESEARCH.md confirms this feature is skipped for Worker-served HTML. No action needed. Record "N/A — Worker-served content exempt."

    4. **Cloudflare Fonts** — leave off (site uses local ClashDisplay woff2). No action.

    5. **Polish** — image optimization only, does not touch HTML. No action.

    Record all 5 audit outcomes in the SUMMARY file (paste them verbatim).
  </what-human-does>
  <resume-signal>
    Type "done" after:
    1. CF Deploy Hook created, test curl triggered a build, build went green
    2. Sanity webhook created + test edit triggered a build, build went green
    3. Auto Minify confirmed absent (screenshot taken); Rocket Loader confirmed OFF
    4. CF Deploy Hook URL is saved somewhere (pass it back so it lands in the SUMMARY — but do NOT paste it into the plan or commit it anywhere; it's a secret trigger URL)

    If stuck:
    - "I don't see Deploy Hooks section" → it's at Settings → Builds → Deploy Hooks (launched 2026-04-01). If truly missing, the Worker may need to be re-linked to Git. Report back.
    - "Sanity webhook fires but no build appears" → check CF dashboard for the Deploy Hook's "last triggered" timestamp; if blank, the URL was pasted wrong. Re-copy.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN + AUTO — Run launch gate (automated smoke + Lighthouse + manual walkthrough)</name>
  <what-built>
    - Production deployment on realestatestrategy.eu (from 10-01)
    - www → apex redirect (from 10-01)
    - Sanity → CF rebuild pipeline (from Task 2)
    - scripts/smoke-test.sh (from Task 1)

    This task runs the launch gate from CONTEXT.md: automated smoke + Lighthouse ≥90 + Nestor's manual walkthrough. All three must pass.
  </what-built>
  <how-to-verify>
    **Part A: Automated smoke (Claude runs this)**
    From repo root:
    ```
    bash scripts/smoke-test.sh https://realestatestrategy.eu
    ```
    Must exit 0 with all PASS lines. If any FAIL, fix the underlying issue before proceeding (common: sitemap path mismatch, og-default.png missing from public/, locale route 404).

    **Part B: Lighthouse mobile ≥ 90 (Claude runs this)**
    ```
    npx -y @lhci/cli@latest collect --url=https://realestatestrategy.eu/ --settings.formFactor=mobile --settings.throttlingMethod=simulate --numberOfRuns=1
    ```
    Then inspect the JSON output in `.lighthouseci/`:
    ```
    node -e "const r=require(require('fs').readdirSync('.lighthouseci').filter(f=>f.endsWith('.json'))[0].replace(/^/,'./.lighthouseci/')); console.log('perf:', r.categories.performance.score)"
    ```
    (Or simpler: open the HTML report LHCI generates and read the Performance score.)

    Performance score must be ≥ 0.90 (90). If lower:
    - Document the score in SUMMARY
    - List top 3 opportunities from the report
    - Decide with user whether to (a) fix-then-ship, (b) ship-anyway with v2.1 perf ticket, or (c) ship-and-revisit. The CONTEXT.md launch-gate says ≥90 is required — but user decides on exceptions.

    Do NOT check the .lighthouseci/ directory into git (add to .gitignore if it lands in the working tree).

    **Part C: Nestor's manual walkthrough (HUMAN)**
    Open https://realestatestrategy.eu/ in a browser and verify:
    1. [ ] DE homepage renders with Clash Display headings + Chivo body, hero visible above fold, no console errors
    2. [ ] Switch to /en — English content, translated nav labels, translated CTA
    3. [ ] Switch to /es — Spanish content, translated nav labels
    4. [ ] MegaNav: burger opens drawer on mobile (simulate narrow viewport), locale switcher works, no JS errors
    5. [ ] Open one blog post — renders, images load, hreflang alternates look right in `<head>`
    6. [ ] View source of `/` — confirm HTML is NOT minified (line breaks + indentation present) → proves INFR-08 / CF not rewriting HTML
    7. [ ] `view-source:https://realestatestrategy.eu/` — grep for `rocketscript` (should be ABSENT → confirms Rocket Loader not rewriting)
    8. [ ] Edit any document in Sanity Studio (e.g., change siteSettings.tagline by one character), publish, wait ~2-3 minutes, reload apex homepage — change visible. Revert the edit after confirming.
    9. [ ] Test www redirect in browser: https://www.realestatestrategy.eu/blog → lands on https://realestatestrategy.eu/blog (path preserved)
    10. [ ] Open CF Dashboard → Deployments tab — confirm "Rollback to this deployment" button is visible on the previous deployment (rollback strategy sanity check, no action needed)
  </how-to-verify>
  <resume-signal>
    Type "launch approved" when all three parts pass (smoke exit 0, Lighthouse perf ≥ 0.90, all 10 walkthrough items checked).

    Or type "launch gate failed: <reason>" to document what blocked launch — we'll fix in a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
End-to-end:
- `bash scripts/smoke-test.sh https://realestatestrategy.eu` → exit 0
- `curl -s https://realestatestrategy.eu/ | head -5` → shows multi-line indented HTML (not minified) — evidence HTML integrity preserved
- `curl -s https://realestatestrategy.eu/ | grep -c rocketscript` → 0
- Sanity edit → CF build → production reflects change, total ≤ 3 min
- Lighthouse mobile Perf score ≥ 0.90
- Nestor confirms manual walkthrough clean

INFR requirements closure:
- INFR-05 (env vars in CF dashboard): closed by 10-01 Task 2b (build vars + secret set)
- INFR-06 (Sanity webhook → CF rebuild): closed by 10-02 Task 2a+2b (Deploy Hook + Sanity webhook wired, tested)
- INFR-07 (custom domain): closed by 10-01 Task 2e (apex custom domain + cert active)
- INFR-08 (Auto Minify disabled): closed by 10-02 Task 2c Step 1 (documented as "verified not applicable, removed by CF 2024-08-05" with screenshot evidence)
</verification>

<success_criteria>
- Sanity publish triggers prod rebuild within 60s of save; deploys within ~3 min total
- HTML byte-preserved (no CF minification/rewriting)
- All 3 locale homepages + sitemap + robots + og-default return 200
- Lighthouse mobile Performance ≥ 90
- Manual walkthrough (10 items) green
- scripts/smoke-test.sh committed and re-runnable
- All 4 INFR-05..08 requirements closed (INFR-08 as documented no-op)
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-deployment/10-02-SUMMARY.md` including:
- CF Deploy Hook URL (redacted — record location only: "saved in 1Password / Nestor's notes"; NEVER commit the raw URL)
- Sanity webhook config exported as a note (dataset, events, filter state)
- CF HTML-rewriting audit table filled in (all 5 features, with states + screenshots or notes)
- Lighthouse mobile Performance score (actual number)
- Smoke-test run log (paste the PASS lines)
- Launch date/time (when Nestor said "launch approved")
- INFR-05 through INFR-08 closure notes, especially INFR-08's "verified not applicable" rationale
- Any deferred items for v2.1 (perf fixes if score was marginal, etc.)
</output>
