---
phase: 08-static-blocks-and-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/sanity/schemas/objects/blockContent.ts
  - src/sanity/schemas/documents/post.ts
  - src/sanity/schemas/index.ts
  - src/lib/sanity.ts
  - src/lib/imageUrl.ts
  - src/lib/tocFromPT.ts
  - src/lib/readingTime.ts
autonomous: true

must_haves:
  truths:
    - "Plan 08-02 can import `getHomepageWithSections` from src/lib/sanity and `urlFor` from src/lib/imageUrl without additional setup"
    - "Plan 08-03 can render callout and highlight marks because blockContent schema declares them and astro-portabletext component map is ready to consume them"
    - "Plan 08-03 can import `getFullPostForLocale`, `getAllPostsForLocale`, and `getAllPostSlugsForLocale` from src/lib/sanity and `extractToc`/`readingTime` helpers without additional setup"
    - "Plan 08-04 can consume seo.ogImage asset reference from every siteSettings GROQ result"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Jibemates purple primary scale + brand neutrals as @theme tokens"
      contains: "--color-primary-700"
    - path: "src/sanity/schemas/objects/blockContent.ts"
      provides: "Named blockContent type with callout + highlight annotations"
      contains: "name: 'blockContent'"
    - path: "src/lib/sanity.ts"
      provides: "getFullPostForLocale, getAllPostsForLocale, getAllPostSlugsForLocale, getSiteSettings, getHomepageWithSections"
      exports: ["getFullPostForLocale", "getAllPostsForLocale", "getAllPostSlugsForLocale", "getSiteSettings", "getHomepageWithSections"]
    - path: "src/lib/imageUrl.ts"
      provides: "urlFor() Sanity image URL builder"
      exports: ["urlFor"]
    - path: "src/lib/tocFromPT.ts"
      provides: "extractToc() helper for h2/h3 headings -> TocItem[]"
      exports: ["extractToc"]
    - path: "src/lib/readingTime.ts"
      provides: "readingTime() locale-aware estimator"
      exports: ["readingTime"]
  key_links:
    - from: "src/sanity/schemas/documents/post.ts"
      to: "src/sanity/schemas/objects/blockContent.ts"
      via: "body field type: 'blockContent'"
      pattern: "type:\\s*'blockContent'"
    - from: "src/sanity/schemas/index.ts"
      to: "blockContentType"
      via: "schema registration export"
      pattern: "blockContentType"
---

<objective>
Lay the foundation for Phase 8: define the Jibemates purple Tailwind v4 palette, extend the Sanity blockContent schema with custom marks (callout, highlight), and add GROQ helpers + utility libraries needed by both blocks and blog.

Purpose: Blocks (Plan 02) and Blog (Plan 03) cannot be built without (a) color tokens, (b) custom marks defined in Sanity, (c) full post / homepage GROQ projections, (d) image URL builder, and (e) TOC/reading-time helpers. Doing this once up front avoids repeating it across plans.

Output: Color tokens live in global.css. New Sanity object schema registered. Five new helper modules exported from src/lib/. TypeGen re-run if applicable.
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-static-blocks-and-blog/08-CONTEXT.md
@.planning/phases/08-static-blocks-and-blog/08-RESEARCH.md

@src/lib/sanity.ts
@src/styles/global.css
@src/sanity/schemas/index.ts
@src/sanity/schemas/documents/post.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Jibemates purple palette + brand neutrals to global.css</name>
  <files>src/styles/global.css</files>
  <action>
Append a `@theme` block (Tailwind v4 syntax) with the OKLCH primary scale and brand neutrals from RESEARCH.md section "Jibemates Purple Color Palette Recommendation":

- `--color-primary-50` through `--color-primary-950` using `oklch(L 0.02..0.20 290)` values exactly as specified in RESEARCH.md (hue 290).
- `--color-brand-dark: #131313`
- `--color-brand-cream: #efeeec`
- `--color-brand-warm-50: #f7f6f4`
- `--color-brand-warm-100: #efeeec`

If a `@theme` block already exists, merge — do NOT duplicate. Preserve existing font-family tokens added in Phase 7.

Add a brief comment header: `/* Jibemates purple primary scale (hue 290 OKLCH). See .planning/phases/08-static-blocks-and-blog/08-RESEARCH.md */`
  </action>
  <verify>
`npm run build` completes without CSS errors. `grep -c 'color-primary-' src/styles/global.css` returns 11 (50,100,200,300,400,500,600,700,800,900,950).
  </verify>
  <done>Tailwind utilities like `bg-primary-700`, `text-primary-300`, `bg-brand-dark` resolve in subsequent block components.</done>
</task>

<task type="auto">
  <name>Task 2: Create blockContent Sanity schema with callout + highlight marks; wire post.body</name>
  <files>
src/sanity/schemas/objects/blockContent.ts
src/sanity/schemas/documents/post.ts
src/sanity/schemas/index.ts
  </files>
  <action>
Create `src/sanity/schemas/objects/blockContent.ts` exporting `blockContentType` exactly per RESEARCH.md "Sanity Schema Changes Required" section. Include:
- decorators: strong, em, code, underline, strike-through
- annotations: link (url), callout (variant: info|warning|tip), highlight (color: yellow|purple)
- styles: normal, h2, h3, blockquote
- second array member: image with hotspot + alt + caption fields

Update `src/sanity/schemas/documents/post.ts`: change the `body` field from inline `type: 'block'` (or current shape) to `type: 'blockContent'`. Preserve all other post fields untouched.

Update `src/sanity/schemas/index.ts`: import `blockContentType` and add it to the `schemaTypes` array (or whichever export the schema registry uses — match existing pattern).

After editing, if a Sanity TypeGen step exists in package.json scripts (look for `typegen`), run it: `npm run typegen` — capture output. If it does not exist, skip.
  </action>
  <verify>
`grep -q "type: 'blockContent'" src/sanity/schemas/documents/post.ts` succeeds. `grep -q "blockContentType" src/sanity/schemas/index.ts` succeeds. `npm run build` (Astro) completes — Sanity schema is consumed by Studio externally so Astro build doesn't depend on it, but ensure no import errors.
  </verify>
  <done>blockContent schema is registered, post.body uses it, callout + highlight annotations are defined.</done>
</task>

<task type="auto">
  <name>Task 3: Add image URL builder and three lib helpers (toc, readingTime)</name>
  <files>
src/lib/imageUrl.ts
src/lib/tocFromPT.ts
src/lib/readingTime.ts
  </files>
  <action>
Create three new files exactly per RESEARCH.md patterns 8, 9, 10:

`src/lib/imageUrl.ts`: import `imageUrlBuilder` from `@sanity/image-url`, import `sanityClient` from `'sanity:client'`. Export `urlFor(source)` returning `builder.image(source)` or `null` for falsy input. Type the source param as `{ asset: { _ref: string } } | null | undefined`.

`src/lib/tocFromPT.ts`: Export `TocItem` interface ({ text, id, level: 2|3 }) and `extractToc(body: unknown[]): TocItem[]` filtering body for `block` type with `style === 'h2' || 'h3'`, joining children text, slugging to id (lowercase, strip non-word, dashes for spaces, max 60 chars).

`src/lib/readingTime.ts`: Export `readingTime(body: unknown[], locale: 'de'|'en'|'es'): number`. Use 200 wpm for de, 225 for en/es. Filter blocks of type 'block', flatMap children spans, join text, count words, ceil(words/wpm), min 1.

Verify `@sanity/image-url` is installed: `npm ls @sanity/image-url` — if missing, run `npm install @sanity/image-url@^2.1.1`.
  </action>
  <verify>
`npx tsc --noEmit` (or `npm run build`) compiles without errors in these three files. `node -e "import('./src/lib/readingTime.ts').then(m => console.log(typeof m.readingTime))"` is not required — type-check via build is sufficient.
  </verify>
  <done>urlFor, extractToc, readingTime are exported and type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Add five GROQ helpers to src/lib/sanity.ts</name>
  <files>src/lib/sanity.ts</files>
  <action>
Append (do NOT replace existing exports — `getPostWithTranslations` must remain unchanged; it serves the Phase 9 locale switcher contract) the following five helpers exactly per RESEARCH.md "GROQ Queries Needed":

1. `getFullPostForLocale(slug: string, locale: Locale): Promise<FullPost | null>` — full post projection (body, mainImage with deep asset, excerpt, publishedAt, author->{name,image}, category, tags, seo, _translations).
2. `getAllPostsForLocale(locale: Locale): Promise<PostCard[]>` — for blog index cards. Fields: _id, title, slug, publishedAt, mainImage, excerpt, author->{name}, category, tags. Order by publishedAt desc.
3. `getAllPostSlugsForLocale(locale: Locale): Promise<Array<{ slug: string }>>` — for `getStaticPaths` in post detail routes. Query: `*[_type == "post" && language == $locale && defined(slug.current)]{ "slug": slug.current }`. Parameterised.
4. `getSiteSettings(): Promise<SiteSettings | null>` — singleton (NO language filter). Fields: siteName, tagline, defaultCtaHref, seo{title,description,ogImage{asset->{_ref,_id}}}, navigation[], footer{socialLinks[]}.
5. `getHomepageWithSections(locale: Locale): Promise<PageWithSections | null>` — homepage with deep section projection for image refs inside landingGuide and landingTestimonials (per RESEARCH.md Pattern in GROQ section).

Define minimal TypeScript interfaces inline (FullPost, PostCard, SiteSettings, PageWithSections) — use `unknown[]` for body and sections arrays; field-level typing is good enough. Reuse the existing `Locale` type already in sanity.ts.

Use parameterised GROQ — never string-interpolate locale.
  </action>
  <verify>
`grep -E "export (async )?function get(FullPostForLocale|AllPostsForLocale|AllPostSlugsForLocale|SiteSettings|HomepageWithSections)" src/lib/sanity.ts` returns 5 lines. `grep -q "getPostWithTranslations" src/lib/sanity.ts` still succeeds (preserved). `npm run build` compiles.
  </verify>
  <done>Five new helpers exported; existing exports preserved; build green.</done>
</task>

</tasks>

<verification>
- `npm run build` completes cleanly.
- `grep -c '@theme' src/styles/global.css` >= 1.
- All five new GROQ helpers exist and are exported.
- blockContent schema registered.
- imageUrl, tocFromPT, readingTime modules exist and type-check.
</verification>

<success_criteria>
Foundation ready for Plan 02 (blocks) and Plan 03 (blog) to consume color tokens, image URL builder, GROQ helpers, and PT utilities without further setup.
</success_criteria>

<output>
After completion, create `.planning/phases/08-static-blocks-and-blog/08-01-SUMMARY.md` documenting:
- Final OKLCH values used (confirm hue 290 scale)
- blockContent schema location + whether TypeGen was re-run
- Five GROQ helpers + their return shapes
- Three lib/ utilities + signatures
</output>
