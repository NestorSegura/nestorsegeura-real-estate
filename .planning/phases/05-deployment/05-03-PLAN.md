---
phase: 05-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/revalidate/route.ts
autonomous: true

must_haves:
  truths:
    - "POST /api/revalidate with invalid signature returns 401"
    - "POST /api/revalidate with valid signature and known _type revalidates the tag and returns 200"
    - "POST /api/revalidate with missing _type returns 400"
  artifacts:
    - path: "src/app/api/revalidate/route.ts"
      provides: "Sanity webhook revalidation endpoint"
      exports: ["POST"]
      min_lines: 20
  key_links:
    - from: "src/app/api/revalidate/route.ts"
      to: "next/cache"
      via: "revalidateTag import"
      pattern: "revalidateTag.*body\\._type"
    - from: "src/app/api/revalidate/route.ts"
      to: "next-sanity/webhook"
      via: "parseBody import"
      pattern: "parseBody"
---

<objective>
Implement the /api/revalidate webhook handler with HMAC verification and tag-based revalidation.

Purpose: When content is published in Sanity, the webhook calls this endpoint, which verifies the signature and revalidates the Next.js cache for the affected document type. This is the final piece connecting Sanity edits to live site updates.
Output: src/app/api/revalidate/route.ts
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployment/05-RESEARCH.md
@src/sanity/lib/live.ts
@src/sanity/schemas/documents/page.ts
@src/sanity/schemas/documents/post.ts
@src/sanity/schemas/documents/siteSettings.ts
@src/sanity/schemas/documents/author.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/revalidate webhook route</name>
  <files>src/app/api/revalidate/route.ts</files>
  <action>
Create `src/app/api/revalidate/route.ts` with:

1. Import `revalidateTag` from `next/cache`
2. Import `NextRequest`, `NextResponse` from `next/server`
3. Import `parseBody` from `next-sanity/webhook`
4. Define `KNOWN_TYPES` array: `['page', 'post', 'siteSettings', 'author']` (verified from src/sanity/schemas/documents/)
5. Export async `POST` handler:
   - Call `parseBody<{ _type: string }>(req, process.env.SANITY_WEBHOOK_SECRET)` to validate HMAC signature
   - If `!isValidSignature`: return `new Response('Invalid signature', { status: 401 })`
   - If `!body?._type`: return `new Response('Bad request', { status: 400 })`
   - Call `revalidateTag(body._type)` — this matches the cache tags used by defineLive/sanityFetch
   - Return `NextResponse.json({ revalidated: true, type: body._type, now: Date.now() })`
   - Wrap in try/catch: on error, log `[revalidate] <message>` and return 500

Published-only: No draft handling needed. defineLive handles preview via draft mode separately.

Do NOT use `NEXT_PUBLIC_` prefix for the webhook secret — it is server-side only.

After creating, verify the endpoint works locally:
```bash
# Test invalid signature returns 401
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/api/revalidate -H "Content-Type: application/json" -d '{"_type":"page"}'
# Should return 401
```
  </action>
  <verify>
Run `npm run dev` in background, then:
1. `curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/api/revalidate` returns 401 (no signature)
2. TypeScript compilation: `npx tsc --noEmit` passes (or `npm run build` succeeds)
  </verify>
  <done>POST /api/revalidate returns 401 for invalid/missing signatures, accepts valid HMAC signatures and revalidates the cache tag for the document type. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `test -f src/app/api/revalidate/route.ts` — file exists
- `grep -q "parseBody" src/app/api/revalidate/route.ts` — uses next-sanity/webhook
- `grep -q "revalidateTag" src/app/api/revalidate/route.ts` — calls revalidateTag
- `grep -q "SANITY_WEBHOOK_SECRET" src/app/api/revalidate/route.ts` — reads correct env var
- `grep -q "401" src/app/api/revalidate/route.ts` — rejects invalid signatures
- `npm run build` completes without TypeScript errors (OPS-07)
</verification>

<success_criteria>
- Webhook endpoint validates HMAC signature via parseBody from next-sanity/webhook
- Invalid signatures get 401, missing _type gets 400
- Valid requests revalidate the correct cache tag and return 200 with JSON response
- No secrets exposed in client bundle (no NEXT_PUBLIC_ prefix)
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment/05-03-SUMMARY.md`
</output>
