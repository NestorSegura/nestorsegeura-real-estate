---
phase: 05-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ecosystem.config.js
  - .env.local.template
autonomous: true

must_haves:
  truths:
    - "PM2 ecosystem config runs .next/standalone/server.js in fork mode"
    - "Process name is nestorsegura.com"
    - "Environment variable template documents all production vars"
  artifacts:
    - path: "ecosystem.config.js"
      provides: "PM2 process configuration"
      contains: "nestorsegura.com"
    - path: ".env.local.template"
      provides: "Updated env var documentation"
      contains: "SANITY_WEBHOOK_SECRET"
  key_links:
    - from: "ecosystem.config.js"
      to: ".next/standalone/server.js"
      via: "script field"
      pattern: "script.*standalone/server.js"
---

<objective>
Create PM2 ecosystem config and update environment variable documentation for production.

Purpose: PM2 manages the Node.js process with auto-restart on crash and server reboot persistence. The env template ensures production environment is correctly configured.
Output: ecosystem.config.js, updated .env.local.template
</objective>

<execution_context>
@/Users/nestorsegura/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nestorsegura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployment/05-RESEARCH.md
@.env.local.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ecosystem.config.js</name>
  <files>ecosystem.config.js</files>
  <action>
Create `ecosystem.config.js` at project root with:

```javascript
module.exports = {
  apps: [
    {
      name: 'nestorsegura.com',
      script: '.next/standalone/server.js',
      exec_mode: 'fork',
      instances: 1,
      env: {
        NODE_ENV: 'production',
        PORT: 3000,
        HOSTNAME: '0.0.0.0',
      },
    },
  ],
}
```

Per user decision: fork mode, single process, process name `nestorsegura.com`.

IMPORTANT: Do NOT put secrets (SANITY_API_READ_TOKEN, SANITY_WEBHOOK_SECRET) in this file — it is committed to git. Secrets go in `.env.production` on the server, which Next.js reads automatically at startup.

Only NODE_ENV, PORT, HOSTNAME belong here.
  </action>
  <verify>Run `node -e "const c = require('./ecosystem.config.js'); console.log(JSON.stringify(c))"` — must output valid JSON with name `nestorsegura.com` and script `.next/standalone/server.js`.</verify>
  <done>ecosystem.config.js exists with fork mode, correct process name, and only non-secret env vars</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.local.template with production notes</name>
  <files>.env.local.template</files>
  <action>
Update `.env.local.template` to add a production deployment section at the bottom. Keep all existing content unchanged. Append:

```
# --- Production Deployment ---
# On the VPS, copy this file to .env.production and fill in production values:
#   cp .env.local.template .env.production
#
# Next.js automatically reads .env.production when NODE_ENV=production.
# NEVER commit .env.production to git.
#
# Required production values:
#   NEXT_PUBLIC_SANITY_PROJECT_ID=0cn4widw
#   NEXT_PUBLIC_SANITY_DATASET=production
#   NEXT_PUBLIC_SITE_URL=https://nestorsegura.com
#   SANITY_API_READ_TOKEN=<from sanity.io/manage>
#   SANITY_WEBHOOK_SECRET=<openssl rand -hex 32>
```

Also verify `.env.production` is in `.gitignore`. If not, add it.
  </action>
  <verify>Grep `.env.local.template` for "env.production" — must find the production section. Grep `.gitignore` for `.env.production` — must be present.</verify>
  <done>.env.local.template documents the production env setup. .env.production is gitignored.</done>
</task>

</tasks>

<verification>
- `node -e "require('./ecosystem.config.js')"` exits 0
- `grep -q "nestorsegura.com" ecosystem.config.js` returns 0
- `grep -q "standalone/server.js" ecosystem.config.js` returns 0
- `grep -q "fork" ecosystem.config.js` returns 0
- `grep -q ".env.production" .env.local.template` returns 0
- `grep -q ".env.production" .gitignore` returns 0
</verification>

<success_criteria>
- PM2 config correctly targets standalone server.js in fork mode
- No secrets in ecosystem.config.js (only NODE_ENV, PORT, HOSTNAME)
- Production env var setup is documented in the template
- .env.production is gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment/05-02-SUMMARY.md`
</output>
