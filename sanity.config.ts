// Root-level re-export required by Sanity CLI for schema extraction and TypeGen.
// Actual config lives at src/sanity/config.ts (colocated with schemas/lib).
export { default } from './src/sanity/config'
